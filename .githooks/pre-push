#!/usr/bin/env bash
# Aether pre-push — refuse a dirty public field even if pre-commit was skipped.
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
exec "$ROOT/scripts/ci-field-hygiene.sh"
